"""Multiplication.

The ``Tests:`` block below is intentionally present but EMPTY: this module
declares no cross-cutting tests, so a change here selects only its mirror
(``test_mul``). It demonstrates that an empty block is valid and is treated
exactly like "no extra targets".

Tests:
"""


def mul(a, b):
    """Return the product of ``a`` and ``b``."""
    return a * b
