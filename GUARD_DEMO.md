# Guard demo (throwaway)

This file exists only to open a PR from a non-`dev` branch into `main`, so the
`guard-main` job can demonstrate rejecting it ("PRs to main must come from dev").
Delete after the demo.
